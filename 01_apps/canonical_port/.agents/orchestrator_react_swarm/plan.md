# Execution Plan: React/Vite Web UI Competitive Swarm & Tri-Orchestrator AI Debate

## Objective
Build out the React/Vite Web UI in `src/` by running a competitive 3-track swarm (Track Alpha: NOC/Hardware Dashboard, Track Beta: Chat/IDE Interface, Track Gamma: Data Lake / Obsidian Graph), evaluating them via the Tri-Orchestrator AI Debate Protocol with mathematical consensus (>0.98), harmonizing the winning components into `src/`, and verifying with zero-mock integrity and full test/build verification.

## Phase 0: Survey & Exploration (Parallel Explorers)
- Spawn 3 `teamwork_preview_explorer` agents:
  1. Explorer 1: Inspect `01_apps/canonical_port` structure, `package.json`, Vite configuration, React dependencies, current `src/` layout, styling (Tailwind/CSS), and existing components.
  2. Explorer 2: Inspect backend API endpoints across the Lauburu ecosystem (Port 18802 self-healing/telemetry, Port 4000 hub, llama.cpp Ports 8081-8084, Qdrant 6333, Obsidian vault, etc.) and data structures to ensure Zero-Mock compliance.
  3. Explorer 3: Inspect existing Canonical Terminal TUI implementations (Python Textual, Go Bubbletea, Rust Ratatui in the monorepo) to extract UI density, layout patterns, hotkeys, status lines, and feature expectations.

## Phase 1: Architecture, PROJECT.md & Dual Track Test Harness
- Aggregate survey findings into `PROJECT.md` Feature Inventory & Architecture.
- Establish E2E test harness track (`teamwork_preview_test_writer`) for Web UI components and build validation.

## Phase 2: Competitive Implementation Tracks (Alpha, Beta, Gamma)
- Track Alpha: NOC / Hardware Dashboard Component (Node topology L1-L7, VRAM/RAM gauges, bandwidth, latency, zero-mock live telemetry).
- Track Beta: Chat / IDE Interface Component (Local llama.cpp REST client, prompt editor, streaming token view, parameter sliders, syntax highlighting).
- Track Gamma: Data Lake / Obsidian Graph Component (Qdrant search, Obsidian vault graph visualization, markdown renderer, LoRA dataset viewer).

## Phase 3: Tri-Orchestrator AI Debate Protocol
- Conduct dynamic multi-round debate between Cloud AI, Local AI, Devil's Advocate, and Training Engine.
- Evaluate prototypes on: Performance & Non-blocking State Management (Web Workers / event loops), UI/UX Density & TUI parity, Code Quality & TypeScript Rigor.
- Produce `canonical_react_verdict.md` with mathematical scoring (>0.98 consensus threshold).

## Phase 4: Harmonization & Production Integration
- Worker merges the highest-scoring architectures and components into `src/App.tsx` and modular components.
- Ensure seamless multi-tab / tiled split-view layout capturing all 3 domains with responsive density.

## Phase 5: Verification, Review, Adversarial Hardening & Forensic Audit
- Spawn Reviewers (`teamwork_preview_reviewer`) for code review & build validation (`npx vite build`).
- Spawn Challengers (`teamwork_preview_challenger`) for performance & stress testing.
- Spawn Forensic Auditor (`teamwork_preview_auditor`) for Zero-Mock Rule #0 and integrity verification.
- Complete milestone gating and report final outcome to parent.
