# Termux Edge Daemon & Network Self-Healing Runtime

Headless Python/Node runtime executing on Android (Pixel 10 / Samsung S20) via Termux.

- Responsibilities: Network ping telemetry, automated interface bounces (`svc wifi/data`), local RAG cache, llama-rpc-server worker.
