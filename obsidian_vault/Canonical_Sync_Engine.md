---
title: "Canonical Sync Engine CLI"
tags: [lauburu, tools, synchronization, quad-vault, cli]
---
# 🚀 Canonical Sync Engine CLI

The Canonical Sync Engine was successfully generated by the multi-agent Teamwork Swarm (Benchmark Integrity Mode) to enforce Rule #0 (Zero-Mock Data) and Rule #6 (Storage Health).

## Architecture
The engine simultaneously manages synchronized state across the Four Canonical Sources of Truth:
1. **PySpark Data Lake** (Local `flock` locks for JSONL training feeds)
2. **Obsidian Vault** (Markdown Graph via Wikilinks)
3. **GitHub Monorepo** (Source code version tree)
4. **Google Drive API** (Cloud persistence via `/Volumes/Google Drive/My Drive`)

## Installation & Path
The source code was committed to Project Memory at:
`/Users/aaron/DFS_UNIFIED/Lauburu-Monorepo/06_scripts_and_tooling/canonical_sync_engine`

The executable is mapped to `~/.local/bin/canonical-sync`.

## Available Commands
- `canonical-sync verify`: Scans mesh and validates storage health across local and remote vaults.
- `canonical-sync heal`: Executes pre-flight self-healing routines per Rule 6.2.
- `canonical-sync sync`: Ingests and synchronizes a TruthArtifact across the Quad-Vault ecosystem.
- `canonical-sync status`: Shows vault health, disk headroom, and mesh node reachability.
